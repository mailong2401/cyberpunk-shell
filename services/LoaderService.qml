import QtQuick
import qs.services
import qs.modules.dashboard

Item {
  /***
  Loader {
  source: "../modules/dashboard/Dashboard.qml"
  active: VisibleService.dashboard
  onLoaded: {
  item.visible = Qt.binding(function () {
  return VisibleService.dashboard;
});
}
}
  ***/
}
